package p2p

// Decode binary packet to Packet
func Decode2Packet(data []byte) (*PacketContent, error) {
	return nil, nil
}

// Decode Packet to rpc
func Decode2RPC(data []byte) (*RPCContent, error) {
	return nil, nil
}

// Decode rpc to message
func Decode2Message(data []byte) (*Message, error) {
	return nil, nil
}
